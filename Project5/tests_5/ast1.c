function int sumFun(int n) {
    for (i = 0; (i < 4) && (digit > -1); i = i + 1) {
        digit = hexdigit2int(hexnum[i]);
        if (digit > -1) {
            number = number * 16 + digit;
        }
    }
}